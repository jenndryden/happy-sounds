import React from 'react';
import ReactDOM from 'react-dom';
import AudioSliderApp from './App';

ReactDOM.render(
  <React.StrictMode>
    <AudioSliderApp />
  </React.StrictMode>,
  document.getElementById('root')
);
